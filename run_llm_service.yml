apiVersion: v1
kind: Service
metadata:
  name: llm-service
  namespace: kc-ai-research-lab
spec:
  selector:
    app: llm-server
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
